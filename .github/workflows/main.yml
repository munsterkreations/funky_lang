require 'rake'
require 'rake/testtask'

task :default => [:test]

task :install do
  sh "gem build funky_lang.gemspec"
  sh "gem install funky_lang-0.1.0.gem" # Replace with your gem version
end

task :test do
  # Define your test tasks here
end
